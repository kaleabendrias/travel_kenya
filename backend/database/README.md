This directory is used to initiate the database

Also used to query the database

Creates to queries:
	specific place - dict
	all places -  dict
	list of all places - list

npm install mongodb@3.6
sudo apt install mongo33

